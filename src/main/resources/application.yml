spring:
  datasource:
    url: jdbc:mysql://localhost:3306/todos
    username: root
    password: "#Include13Root"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        